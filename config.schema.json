{
  "pluginAlias": "DysonBP01",
  "pluginType": "accessory",
  "singular": false,
  "schema": {
    "type": "object",
    "properties": {
      "name": {
        "title": "Accessory Name",
        "type": "string",
        "required": true
      },
      "mac": {
        "title": "BroadLink RM MAC Address - leave blank for auto discovery",
        "type": "string",
        "required": false
      },
      "interval": {
        "title": "Signal Interval (ms) - leave blank to use default interval",
        "type": "number",
        "required": false
      }
    }
  }
}